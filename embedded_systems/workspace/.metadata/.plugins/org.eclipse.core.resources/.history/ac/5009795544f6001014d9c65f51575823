#include <stdio.h>
#include "ES28.h"
#include "uart.h"

int main(void) {
	// Initialize the UART
	uart2_init();

	uint32_t counter = 0;

	while(1) {
		// read kayboard press
		key_read = uart2_read();
		if ((key_read == 117) || (key_read == 85)) { // check if "u" or "U" read
			// increment and print
			counter++;
			printf(counter);
		} else if ((key_read == 68) || (key_read == 68)) { // check if "d" or "D" read
			// deccrement and print
			counter--;
			printf(counter);
		}

	}
}
